## The str() function converts its input object into a string. the len() function, which returns the number of elements in an objec

device_id_length = len("h32rb17")
if device_id_length == 7:
    print("The device ID has 7 characters.")