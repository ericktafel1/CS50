## The sorted() function sorts the components of a list. The sorted() function also works on any iterable, like a string, and
## returns the sorted elements in a list. The sorted() function does not change the iterable that it sorts.

time_list = [12, 2, 32, 19, 57, 22, 14]
print(sorted(time_list))
print(time_list)